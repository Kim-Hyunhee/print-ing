import "reflect-metadata";
import { Homepage } from "../entity";
import { getRepository } from "typeorm";

const HomepageService = {
  createHomepage: async () => {},
  putHomepage: async () => {},
  getHomepage: async () => {},
};
export default HomepageService;
