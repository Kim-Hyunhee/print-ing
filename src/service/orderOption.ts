import "reflect-metadata";
import { OrderOption } from "../entity";
import { getRepository } from "typeorm";

const OrderOptionService = {
  createOrderOption: async () => {},
  putOrderOption: async () => {},
  deleteOrderOption: async () => {},
  patchOrderOption: async () => {},
};
export default OrderOptionService;
